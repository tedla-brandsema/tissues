{{define "login"}}
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Sign in with Identity Platform</title>
    {{template "importmap" .}}
    {{template "ui-bootstrap" .}}
  </head>
  <body>
    <tissues-auth-login base="{{.BasePath}}"></tissues-auth-login>
  </body>
</html>
{{end}}
